/* what should be used in login form:-
    console.log()----two methods
    var,let,const,
    operators (+ - * / %),
    boolean values,
    Math.round,
    typeof,
    conditionals---
                    <, >, <=, >=, ==, !=, ===, !===, &&, || 
                    if, if....else,switch
                    ternary operator--- condition ? exprIfTrue : exprIfFalse
    Functions,
    Hoisting concept
    ScopeChaining (Popsicle) 
    array---pop, unshift, shift, Array.from, ___.indexOf
    return keyword
    filter, Slicing and Splicing,
    Objects,
    Loops:- for loop, while loop, do while loop, for of loop, for in loop
    forEach
    this keyword
*/